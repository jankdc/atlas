fn main(args: Int): Int

  let name = foo(2)
  let mut john = fibo(45)

  fn foo(n: Int): Unit
    pass

  23

fn identity(fibo: Int -> Int): Int -> Int
  let mut other = fibo(23)
  fibo

fn fibo(n: Int): Int
  n

fn fibo(m: Int, h: Int): Int
  1234
  m
  h
